#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    // Complete the code.
    char ch;
    long x;
    float fl;
    double db;
    int i;
    
    scanf("%d %ld %c %f %lf", &i, &x, &ch, &fl, &db);
    printf("%d\n%ld\n%c\n%f\n%lf", i, x, ch, fl, db);
    
    return 0;
}
